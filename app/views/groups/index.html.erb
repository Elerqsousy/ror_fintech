<h1>Listing groups</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Image data</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @groups.each do |group| %>
  <tr>
    <td><%= group.name %></td>
    <td><%= group.image %></td>
    <td><%= link_to 'Show', [group.user, group] %></td>
    <td><%= link_to 'Edit', edit_user_group_path(group.user, group) %></td>
    <td><%= link_to 'Destroy', user_group_path(current_user, group), method: :delete, data: { turbo_method: :delete, confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Group', new_user_group_path(params[:user_id]) %>
