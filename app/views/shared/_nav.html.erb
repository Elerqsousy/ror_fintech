<% if !current_page?(root_path)%>
<nav class="navbar navbar-expand-lg navbar-dark bg-prim-gradient py-2">
  <div class="container-fluid container">
    <button class="navbar-toggler border-0 p-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
        <%= link_to "Recipe App", root_path, class: 'navbar-brand'%>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <% if user_signed_in?%>
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <%= link_to "Public Recipes", root_path, class: "nav-link"%>
          </li>

          
        </ul>
        <div class="d-flex">
          <%= button_to "Log Out", destroy_user_session_path, method: :delete, class: 'btn btn-outline-danger' %>
        </div>
      <% end %>
    </div>
  </div>
</nav>
<% end %>
